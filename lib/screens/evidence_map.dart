import 'package:flutter/material.dart';

class EvidenceMapWidget extends StatelessWidget {
	const EvidenceMapWidget({super.key});

	@override
	Widget build(BuildContext context) {
		return const Center(
			child: Text('Evidence Map'),
		);
	}
}

